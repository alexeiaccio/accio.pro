import styled from 'styled-components'

export const Button = styled.span`
  align-items: center;
  border-radius: .5rem;
  display: inline-flex;
  height: 3rem;
  justify-content: center;
  overflow: hidden;
  padding: .5rem 1.5rem;
`